# MaxBounty Affiliate API credentials
# Copy this file to .env and fill in your values.
# .env is gitignored — never commit your real credentials.
#
# These are your MaxBounty dashboard login credentials.
# Used by: scripts/fetch-tracking-links.mjs
MB_EMAIL=you@example.com
MB_PASSWORD=yourpassword
